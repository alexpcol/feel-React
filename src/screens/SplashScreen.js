import React, { Component } from 'react';
import { View, Text, StyleSheet, SafeAreaView, ScrollView } from 'react-native';

type Props = {};

export default () => <View style={styles.screen} />;

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: '#4A90E2',
  },
});
